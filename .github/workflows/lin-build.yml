name: lin-build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: |
        cd build/lin
        make
    - uses: actions/upload-artifact@v2
      with:
        name: clap-example.clap
        # path: build/lin...
